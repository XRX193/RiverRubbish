# 河道垃圾智能处置平台设计方案

## 1. 目标与范围

本项目为巡河人员、调度员和管理员提供河道垃圾发现、识别、研判、派单、清理和核验的闭环管理能力。

系统接收现场照片，将其发送至部署在另一台服务器的河道垃圾识别 FastAPI；在获得结构化识别结果后，调用经 Sub2 转换、兼容 OpenAI Chat Completions 协议的大模型接口，生成统一分类、风险判断、处置优先级和建议。模型只分析后端校验后的结构化识别结果，不接收原始图片。

首版部署目标是本地开发和一台 Ubuntu 24.04 LTS 服务器上的私用访问。应用、`code-server` 均只监听 `127.0.0.1`，通过 SSH 隧道访问。远程垃圾识别 FastAPI 通过私有网络或 VPN 的 HTTPS 接口访问。

本项目包含：

- 账号登录、角色权限和网页管理端。
- 河段档案、现场照片上传、位置和拍摄时间确认。
- 异步图片识别与大模型分析任务。
- 地图、任务查询、人工复核和疑似重复点位提示。
- 中高风险垃圾的处置单、派单、清理凭证和核验闭环。
- 数据统计、CSV 导出、审计日志、备份和本地/服务器运行说明。

首版不包含公开注册、对公网开放服务、短信/邮件/企业微信通知、自动派人、自动确认清理、地图数据落库或复杂 PDF 报表。

## 2. 总体架构

```text
巡河员 / 调度员 / 管理员浏览器
          |
          | HTTP (127.0.0.1，经 SSH 隧道)
          v
FastAPI Web 服务
  |- 登录、权限、上传、查询、管理端、地图、CSV
  |- SQLite: 用户、任务、业务结论、处置单、审计日志
  |- 本地文件: 暂存识别原图、清理凭证、备份
  `- 创建 queued 图片分析任务
          |
          v
独立 Worker（单进程、单任务）
  |- 校验/读取照片 EXIF
  |- HTTPS + API Key 上传图片至远程垃圾识别 FastAPI
  |- 保存已校验的结构化识别结果
  |- 调用 Sub2 OpenAI 兼容 LLM 接口
  |- 生成业务结论，创建处置单或进入待核查
  `- 清理原始识别图片
          |
          +----------------------------+
          |                            |
          v                            v
远程河道垃圾识别 FastAPI          Sub2 通用大模型接口
（另一台 GPU 推理服务器）          （OpenAI 兼容 Chat Completions）
```

Web 服务不执行耗时推理或模型请求。它只负责快速创建任务、返回任务 ID、提供查询和业务操作。Worker 负责领取任务和完成所有外部调用，因而服务重启不会丢失正在排队的工作。

## 3. 技术选型

| 领域 | 选择 |
| --- | --- |
| Web 后端与 REST API | FastAPI + Uvicorn |
| 页面 | 服务端 HTML 模板 + 原生 JavaScript + CSS |
| ORM 与迁移 | SQLAlchemy + Alembic |
| 数据库 | SQLite（单机、单 Worker） |
| 密码哈希 | Argon2 |
| 登录会话 | JWT，存放于 `HttpOnly` Cookie，默认 8 小时有效 |
| 异步执行 | 独立 Python Worker + SQLite 租约队列 |
| 图片处理 | Pillow，用于格式、实际解码和像素限制校验；EXIF 读取 |
| HTTP 客户端 | HTTPX |
| 地图 | Leaflet + OpenStreetMap 瓦片 |
| 远程识别调用 | 配置化 multipart/form-data 适配器 |
| 大模型调用 | OpenAI 兼容 `POST /v1/chat/completions` |
| 本地存储 | SQLite、应用运行目录、受控上传目录 |
| 服务器进程管理 | systemd |

## 4. 用户、角色与权限

系统不开放公开注册。首个管理员由本地命令创建；管理员在网页管理端创建、禁用、重置密码和分配角色。账号不物理删除，以保留任务归属与审计历史。

| 操作 | 巡河员 | 调度员 | 管理员 |
| --- | :---: | :---: | :---: |
| 登录、修改自己的密码 | 是 | 是 | 是 |
| 上传巡河照片并确认时间/位置 | 是 | 是 | 是 |
| 查看自己上传的完整分析报告 | 是 | 是 | 是 |
| 查看分配给自己的处置单 | 是 | 可查看业务范围 | 可查看全部 |
| 更新为清理中、上传清理后照片、提交待核验 | 仅本人负责的单据 | 是 | 是 |
| 派单、改派、设期限、取消处置单 | 否 | 是 | 是 |
| 人工复核、修正最终业务结论、核验清理 | 否 | 是 | 是 |
| 查看全局任务、地图、统计和 CSV 导出 | 否 | 业务范围 | 是 |
| 管理账号、河段、系统配置和运行状态 | 否 | 否 | 是 |

调度员派单时，负责人必须从已启用的巡河员账号中选择。调度员不能管理账号或系统配置。

## 5. 核心业务流程

### 5.1 巡河上报与分析

1. 巡河员登录后选择已有河段，选择一张 JPEG、PNG 或 WebP 照片。
2. 浏览器读取可用 EXIF 信息作为表单初值；用户确认或修改拍摄时间和点位。河段必须从档案中选择。
3. 后端复核文件签名、MIME 类型、真实解码结果、文件大小和像素数，保存任务与受控临时文件，状态为 `queued`。
4. 前端每 2 秒查询任务状态。一次选择多张图片时，客户端拆分为多个“一图一任务”提交。
5. Worker 领取任务，设置 10 分钟租约，状态变为 `processing_fastapi`。
6. Worker 上传图片至远程识别 FastAPI，校验并保存符合契约的结构化结果。
7. Worker 聚合检测目标并调用大模型，状态变为 `processing_llm`。
8. Worker 保存模型结论与最终业务结论：低置信度或关键字段缺失进入 `待核查`；其余任务完成。
9. 最终为中、高风险且不待核查的任务自动创建“待派单”处置单。
10. 远程识别成功且必要结果持久化后立即删除原始识别图片。失败图片保留到任务最终失败或最多 24 小时后清理。

### 5.2 人工复核

调度员处理 `待核查` 任务时可选择：`确认有效`、`误报`、`需重新拍摄`。

确认有效时可修正统一垃圾类别、风险等级和处置优先级，并填写理由。模型的原始结论与 FastAPI 原始识别结果均不可覆盖；系统另存“最终业务结论”。地图、派单、统计和期限计算都使用最终业务结论。

如果确认有效后的最终风险为中或高、且任务没有处置单，系统自动创建“待派单”处置单。

### 5.3 处置闭环

```text
中/高风险且无需待核查
            |
            v
        待派单
            |
            v
        已派单 ---- 改派 ----> 已派单
            |
            v
        清理中
            |
            v
上传清理后照片并提交 -> 待核验
            |                    |
            |                    +-- 核验通过 --> 已清理
            |                    |
            |                    `-- 退回整改 --> 清理中
            |
            `--------------------> 已取消（仅调度员/管理员）
```

- 高风险默认清理期限为 24 小时，中风险默认 72 小时；调度员派单时可调整并记录理由。
- 处置优先级为 `紧急`、`高`、`普通`：分别默认对应高风险、其余中风险、低风险或人工确认后需要跟进的事项。
- 逾期和临近逾期是基于期限计算的显示标记，不是独立状态。首版仅在调度工作台和地图中高亮，不接外部通知。
- 清理负责人上传至少一张清理后照片及实际完成时间，单据进入 `待核验`。调度员或管理员只能“通过”或“退回整改”；退回必须填写理由，并记录核验人和时间。

## 6. 分类、风险与模型分析规则

### 6.1 平台统一垃圾类别

FastAPI 原始类别须原样保留。平台将其映射为下列统一类别，供页面、地图、规则和统计使用：

- 塑料制品
- 泡沫
- 生活垃圾
- 渔业废弃物
- 漂浮植物/自然物
- 危险废弃物疑似
- 其他/无法判断

类别映射在配置或适配器中维护，不将远程 API 的字段名称传播到业务层。

### 6.2 风险规则

风险由确定性规则先生成，大模型只负责解释与建议：

- `危险废弃物疑似`：强制高风险。
- 置信度低于配置阈值、类别或置信度缺失：标记 `待核查`，不自动下业务结论。
- 其余任务综合统一类别、可信检测数量和覆盖程度计算低/中/高风险。
- FastAPI 有边界框或面积时，可按总面积占图比例计算覆盖程度；无该数据时将覆盖程度记为未知，不让模型编造。

### 6.3 大模型输入与输出

模型输入仅包括经过后端校验和聚合的 JSON，例如检测目标、原始类别、统一类别、置信度、数量、覆盖程度、河段、拍摄时间和位置来源。模型不会获得原始图片、密码、API Key 或完整未经校验的上游响应。

系统提示词由 `LLM_SYSTEM_PROMPT` 配置，必须要求模型：

- 仅依据给定事实分析，信息不足时明确写出“信息不足”。
- 不修改或虚构检测目标、类别、数量、位置和置信度。
- 输出固定 JSON；字段为 `summary`、`findings`、`risks`、`recommendations`、`confidence`。
- 将风险解释、处置建议与需人工核查原因写清楚。

模型响应不符合 JSON Schema 时自动重试一次。仍不合格则记录 LLM 阶段失败；网络超时、HTTP 429、HTTP 5xx 等可重试错误最多指数退避重试 3 次。手动重试 LLM 只复用已保存的 FastAPI 结果，不重复上传图片或占用远程 GPU。

## 7. 图片、位置和时间规则

| 项目 | 规则 |
| --- | --- |
| 单任务图片数 | 一张；页面可多选并拆分多个任务 |
| 支持格式 | JPEG、PNG、WebP |
| 文件大小 | 最大 10 MB |
| 解码像素 | 最大 2,000 万像素 |
| 校验方式 | 文件签名、MIME、Pillow 实际解码三重校验 |
| 河段 | 必填，从管理员维护的河段档案选择 |
| 坐标 | 优先 EXIF GPS；可在地图点选或手动覆盖；无坐标不阻止上报 |
| 时间 | 优先 EXIF 拍摄时间；缺失则上传时间；用户可修正 |
| 数据留存 | 保存 EXIF 原始值、用户确认值、最终采用值和来源 |

用户手动修改坐标时必须写简短原因。地图与调度统一使用最终确认坐标；EXIF 原始坐标保留作核查依据。

### 7.1 疑似重复点位

系统不自动合并照片。若同一河段内，最终确认坐标距离不超过 50 米、拍摄时间相差不超过 24 小时、最终统一类别相同，则将任务提示为“疑似重复点位”。调度员可手动关联到既有处置单；系统绝不自动关闭、取消或合并任务。

## 8. 任务状态与恢复机制

图片分析任务状态：

| 状态 | 含义 |
| --- | --- |
| `queued` | 已校验并等待 Worker 领取 |
| `processing_fastapi` | 正在调用远程垃圾识别 FastAPI |
| `processing_llm` | 已获得识别结果，正在调用大模型 |
| `completed` | 已生成结果和最终业务结论，或待人工复核 |
| `failed` | 不可恢复或超过重试次数的失败 |
| `cancelled` | 处理尚未完成时被调度员/管理员取消 |

Worker 每秒领取一个任务，一次只处理一个任务。领取时使用数据库事务设置 10 分钟租约；Worker 重启或异常退出后，超过租约的处理中任务将被回收并按重试规则继续。前端仅轮询查询接口，不会直接触发模型或上游推理。

远程 FastAPI 调用使用连接超时 10 秒、总超时 120 秒，并在请求头携带 `Idempotency-Key: <platform-task-id>`。上游必须按该 Key 保持幂等，使网络超时的重传不会产生重复 GPU 推理或不一致结果。

## 9. 数据模型

以下为首版核心表。所有表都带 `created_at`、`updated_at`，必要时带 `created_by` 或操作人字段。

| 表 | 关键字段 | 用途 |
| --- | --- | --- |
| `users` | `id`、`username`、`password_hash`、`role`、`is_active` | 账号与角色；密码仅保存 Argon2 哈希 |
| `river_segments` | `id`、`code`、`name`、`area`、`description`、`geojson`、`is_active` | 河段档案；禁用而非删除 |
| `inspection_tasks` | `id`、`reporter_id`、`river_segment_id`、`status`、`lease_until`、`retry_count`、`failure_code` | 图片处理队列与流程状态 |
| `task_media` | `task_id`、`storage_path`、`kind`、`expires_at`、`sha256` | 识别原图临时文件和清理凭证文件 |
| `observations` | `task_id`、`captured_at`、`captured_at_source`、`exif_lat/lng`、`confirmed_lat/lng`、`final_lat/lng`、`location_change_reason` | 拍摄时间、位置及其来源 |
| `fastapi_results` | `task_id`、`request_id`、`normalized_result_json`、`raw_result_json`、`schema_version` | 上游原始响应与已校验、标准化结果 |
| `detections` | `task_id`、`source_category`、`normalized_category`、`confidence`、`count`、`bbox_json`、`area_ratio` | 多目标检测明细 |
| `llm_analyses` | `task_id`、`model`、`prompt_version`、`analysis_json`、`status`、`error_summary` | 结构化模型结论与失败信息 |
| `business_conclusions` | `task_id`、`risk_level`、`priority`、`review_status`、`reviewed_by`、`review_reason` | 模型初始结论和人工最终结论 |
| `disposal_orders` | `id`、`task_id`、`status`、`assignee_id`、`due_at`、`completed_at`、`verification_by`、`verification_reason` | 派单和清理闭环 |
| `order_evidence` | `order_id`、`media_id`、`submitted_by`、`submitted_at` | 清理后照片凭证 |
| `audit_logs` | `actor_id`、`action`、`entity_type`、`entity_id`、`before_json`、`after_json`、`reason` | 关键操作审计 |

`fastapi_results.raw_result_json` 仅作为本地开发与管理员排障信息保存，不直接显示给普通用户；日志不写入完整原始响应。

## 10. 上游 FastAPI 契约与适配器

上游具体路径、上传字段和结果字段尚未确定，业务代码必须通过配置化适配器接入。默认值只作本地联调起点：

```text
POST {FASTAPI_BASE_URL}/process
Content-Type: multipart/form-data
文件字段: file
认证: Authorization: Bearer {FASTAPI_API_KEY}
幂等键: Idempotency-Key: {inspection_task_id}
```

最小响应契约：

```json
{
  "success": true,
  "request_id": "upstream-request-id",
  "result": {
    "detections": [
      {
        "category": "plastic_bottle",
        "confidence": 0.94,
        "count": 3,
        "bbox": [100, 40, 180, 200],
        "area_ratio": 0.03
      }
    ]
  },
  "error": null
}
```

最小必填信息是检测类别和置信度；数量、边界框、面积或覆盖率可选。适配器负责：

1. 从 `FASTAPI_RESULT_PATH` 提取有效结果节点，例如 `data.result`。
2. 使用字段映射将上游响应转换为内部 `detections` 结构。
3. 校验类别、置信度范围、数量和边界框合法性。
4. 将缺少关键字段或低置信度的任务送入待核查，不让 LLM 补猜。
5. 在上游接口确定后，只改 `.env` 或特定适配器，不改队列、权限和业务流程。

## 11. REST API 轮廓

页面和 API 均由同一 FastAPI 应用提供。以下为主要路由，实际实现应使用 Pydantic 请求/响应模型与权限依赖。

| 方法 | 路径 | 权限 | 用途 |
| --- | --- | --- | --- |
| `POST` | `/auth/login` | 匿名 | 登录并写入会话 Cookie |
| `POST` | `/auth/logout` | 已登录 | 退出 |
| `POST` | `/auth/password` | 已登录 | 修改自己的密码 |
| `GET` | `/api/me` | 已登录 | 当前用户与角色 |
| `POST` | `/api/tasks` | 已登录 | 上传一张巡河照片并创建任务 |
| `GET` | `/api/tasks` | 按角色过滤 | 任务列表与筛选 |
| `GET` | `/api/tasks/{id}` | 所有者/调度员/管理员 | 任务、检测、分析和结论详情 |
| `POST` | `/api/tasks/{id}/retry` | 所有者或调度员/管理员 | 重试失败阶段 |
| `POST` | `/api/tasks/{id}/cancel` | 调度员/管理员 | 取消未完成任务 |
| `POST` | `/api/tasks/{id}/review` | 调度员/管理员 | 人工复核和最终业务结论 |
| `GET` | `/api/map/points` | 调度员/管理员 | 地图筛选后的点位 GeoJSON |
| `GET` | `/api/statistics` | 管理员 | 统计数据 |
| `GET` | `/api/exports/tasks.csv` | 管理员 | CSV 导出 |
| `GET/POST/PATCH` | `/api/segments...` | 管理员 | 河段维护 |
| `GET/POST/PATCH` | `/api/users...` | 管理员 | 账号、角色、启禁用、密码重置 |
| `POST` | `/api/orders/{id}/assign` | 调度员/管理员 | 派单、改派和期限 |
| `POST` | `/api/orders/{id}/start` | 被指派巡河员/调度员/管理员 | 标记清理中 |
| `POST` | `/api/orders/{id}/evidence` | 被指派巡河员/调度员/管理员 | 上传清理凭证并提交待核验 |
| `POST` | `/api/orders/{id}/verify` | 调度员/管理员 | 核验通过或退回整改 |

## 12. 管理端与工作台

### 巡河员工作台

- 新建上报：河段选择、图片、时间、地图点位确认和修改理由。
- 我的上报：处理状态、完整分析报告、待核查结果和失败后的手动重试。
- 我的处置单：待处理、清理中、退回整改；可上传清理后照片并提交核验。

### 调度员工作台

- 待派单、待核验、临期、已逾期、待核查任务的优先列表。
- 地图筛选：河段、日期、类别、风险、优先级和处置状态；相邻点位聚合；点击点位打开详情。
- 复核模型结论、创建/派发/改派处置单、设置期限、退回整改或核验通过。
- 疑似重复点位提示和关联既有处置单。

### 管理员管理端

- 全局运行状态、任务概览、统计和 CSV 导出。
- 用户创建、角色分配、启用/禁用和密码重置。
- 河段档案维护，包括可选 GeoJSON 范围。
- 脱敏错误摘要、失败次数、重试和清理操作；不在网页中提供识别原图的浏览或下载。

## 13. 存储、日志、审计和保留策略

| 数据 | 保存期限 | 说明 |
| --- | --- | --- |
| 识别原图 | FastAPI 成功后立即删除；失败时最长 24 小时 | 不备份 |
| 任务、检测结果、最终业务结论 | 30 天 | 支撑查询、复核和调度 |
| 清理凭证、处置单 | 1 年 | 处置闭环证据；可定期或管理员手动清理 |
| SQLite 数据库、业务配置 | 每日备份，保留 14 天 | 使用 SQLite 在线备份，避免不一致副本 |
| 审计日志 | 至少与关联业务记录相同期限 | 记录操作人、时间、前后值和理由 |

运行日志只记录任务 ID、用户 ID、状态流转、外部 HTTP 状态、耗时和重试次数。不得记录密码、JWT、API Key、原图内容或完整未经脱敏的上游响应。

## 14. 配置与目录

`.env` 不提交至 Git。所有地址、Key、字段映射、阈值和目录均由配置提供。

```dotenv
# Web 与数据
APP_HOST=127.0.0.1
APP_PORT=8000
APP_SECRET_KEY=replace-with-a-long-random-value
JWT_EXPIRE_HOURS=8
DATABASE_URL=sqlite:////home/deploy/services/image-analysis/data/app.db
UPLOAD_DIR=/home/deploy/services/image-analysis/uploads
EVIDENCE_DIR=/home/deploy/services/image-analysis/evidence
BACKUP_DIR=/home/deploy/services/image-analysis/backups

# 上传限制与队列
MAX_UPLOAD_BYTES=10485760
MAX_IMAGE_PIXELS=20000000
WORKER_POLL_SECONDS=1
TASK_LEASE_SECONDS=600
MAX_RETRIES=3

# 远程河道垃圾识别 FastAPI
FASTAPI_BASE_URL=https://garbage-api.internal.example
FASTAPI_PROCESS_PATH=/process
FASTAPI_FILE_FIELD=file
FASTAPI_API_KEY=replace-with-service-key
FASTAPI_RESULT_PATH=result
FASTAPI_CA_BUNDLE=/home/deploy/services/image-analysis/certs/internal-ca.pem
FASTAPI_CONNECT_TIMEOUT_SECONDS=10
FASTAPI_TIMEOUT_SECONDS=120

# Sub2 OpenAI 兼容大模型 API
LLM_BASE_URL=https://sub2.example/v1
LLM_API_KEY=replace-with-platform-key
LLM_MODEL=replace-with-model-name
LLM_SYSTEM_PROMPT=Only analyze the supplied structured river-waste detection data. Return valid JSON only.

# 业务规则
LOW_CONFIDENCE_THRESHOLD=0.60
HIGH_RISK_DUE_HOURS=24
MEDIUM_RISK_DUE_HOURS=72
```

服务器运行目录建议：

```text
/home/deploy/
  workspace/image-analysis-app/        # Git 代码仓库，供 code-server 编辑
  services/image-analysis/
    .env                               # 仅部署用户可读，不入 Git
    .venv/                             # Python 虚拟环境
    data/app.db                        # SQLite 数据库
    uploads/                           # 识别原图临时目录
    evidence/                          # 清理凭证
    backups/                           # 每日备份
    logs/                              # 可选文件日志
    certs/internal-ca.pem              # 私有 CA（如需要）
```

代码发布时更新 `workspace/image-analysis-app`，执行迁移和测试，然后重启 systemd 服务；不要将 `.env`、数据库、上传目录或备份纳入 Git 或覆盖到代码目录。

## 15. 本地开发与服务器部署

### 15.1 本地开发

提供以下命令作为项目入口：

```bash
# 建立环境与依赖
python -m venv .venv
.venv/bin/pip install -r requirements.txt

# 初始化/升级数据库
.venv/bin/alembic upgrade head

# 首个管理员（交互式读取密码，不回显、不写入配置）
.venv/bin/python -m app.create_user --role admin

# Web 服务（仅本机）
.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000

# 独立 Worker
.venv/bin/python -m app.worker

# 备份
.venv/bin/python -m app.backup
```

Windows 本地开发使用等价的 `.venv\\Scripts\\...` 路径。开发和运行分别启动 Web 与 Worker 两个进程。

### 15.2 Ubuntu 服务器

- 使用非 root 的 `deploy` 用户，允许其通过 `sudo` 安装依赖和管理服务。
- SSH 使用部署用户的高强度独立密码；禁止 root SSH 登录。
- `code-server` 使用独立密码，不复用 SSH 密码，只监听 `127.0.0.1`。
- Web 服务和 Worker 也只监听 `127.0.0.1`；浏览器使用 SSH 端口转发访问，不对公网开放。
- 远程 FastAPI 必须使用 HTTPS + 服务端 API Key；验证受信任 CA 或配置的内部 CA，不能关闭 TLS 校验。

访问示例：

```bash
# 在个人电脑执行，将服务器本机服务映射到本地端口
ssh -L 8000:127.0.0.1:8000 -L 8080:127.0.0.1:8080 deploy@your-server

# 浏览器访问
# http://127.0.0.1:8000      应用
# http://127.0.0.1:8080      code-server
```

建议 service 文件：

```ini
# /etc/systemd/system/river-rubbish-web.service
[Unit]
Description=River rubbish web service
After=network.target

[Service]
User=deploy
WorkingDirectory=/home/deploy/workspace/image-analysis-app
EnvironmentFile=/home/deploy/services/image-analysis/.env
ExecStart=/home/deploy/services/image-analysis/.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
```

```ini
# /etc/systemd/system/river-rubbish-worker.service
[Unit]
Description=River rubbish worker
After=network.target river-rubbish-web.service

[Service]
User=deploy
WorkingDirectory=/home/deploy/workspace/image-analysis-app
EnvironmentFile=/home/deploy/services/image-analysis/.env
ExecStart=/home/deploy/services/image-analysis/.venv/bin/python -m app.worker
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
```

激活与查看日志：

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now river-rubbish-web river-rubbish-worker
sudo systemctl status river-rubbish-web river-rubbish-worker
journalctl -u river-rubbish-worker -f
```

每日备份可由 systemd timer 或部署用户的 cron 运行。备份内容应排除已过期的识别原图。

## 16. 实施顺序与验收

### 阶段 1：基础运行骨架

1. 初始化项目结构、依赖、`.env.example`、SQLite、Alembic 与应用配置。
2. 实现用户、Argon2 登录、JWT Cookie、三种角色和首管理员命令。
3. 实现河段管理、巡河照片上传、图片校验、EXIF 读取和点位确认。

验收：管理员能创建账号和河段；巡河员能登录、选择河段、上传合法图片；非法格式、超限文件和未选择河段被拒绝。

### 阶段 2：异步识别与模型分析

1. 实现 SQLite 租约任务队列与单 Worker。
2. 实现配置化远程 FastAPI 适配器、幂等键、超时、重试与结果校验。
3. 实现检测聚合、类别映射、风险规则和 Sub2 OpenAI 兼容模型客户端。
4. 实现固定 JSON Schema 解析、LLM 重试、失败与手动重试。

验收：任务按状态流转；Worker 重启后租约过期任务可继续；FastAPI 成功后只重试 LLM 不会重复上传图片；模型非法输出不会写入业务结论。

### 阶段 3：调度与闭环

1. 实现最终业务结论、人工复核、疑似重复提示和中高风险自动建单。
2. 实现处置单派单、改派、期限、清理凭证、待核验、通过与退回整改。
3. 实现各角色工作台与权限测试。

验收：巡河员无法查看无关单据或核验；调度员可完整处理待核查与处置流程；人工结论覆盖业务展示但不覆盖模型和 FastAPI 原始结果。

### 阶段 4：管理、地图与运维

1. 实现地图、筛选、逾期高亮、统计和 CSV 导出。
2. 实现审计日志、过期图片清理、清理凭证保留策略和每日备份。
3. 提供 systemd 服务、部署文档与 code-server 访问说明。

验收：地图只显示有最终坐标的任务；管理员能筛选并导出；数据库可从每日备份恢复；服务重启后 Web、Worker 自动恢复。

## 17. 必测场景

- JPEG、PNG、WebP 正常上传；伪装 MIME、损坏图片、超 10 MB、超像素图片被拒绝。
- EXIF GPS/时间的读取、用户覆盖、无 EXIF、无坐标和坐标修改理由。
- 远程 FastAPI 的成功、400、429、5xx、超时、非 JSON、缺字段和重复幂等请求。
- 多检测目标的类别映射、面积聚合、低置信度待核查和危险废弃物强制高风险。
- LLM 正常 JSON、无效 JSON、超时和只重试 LLM 的恢复路径。
- 任务租约到期回收、Worker 中断与取消竞态。
- 三种角色的对象级权限：巡河员只能查看自己的任务与负责单据。
- 自动建单、派单、改派、期限计算、临期/逾期显示、凭证上传、核验通过和退回整改。
- 疑似重复点位只提示、不自动合并的行为。
- 管理端用户禁用后无法登录、河段禁用后不能新建任务且历史关系仍可查询。
- SQLite 在线备份、恢复演练和原图/凭证的过期清理。

## 18. 实现边界

远程识别 FastAPI 的最终 URL、上传字段、认证头和响应字段尚未确定。实现时需先以 mock 服务验证适配器，然后根据真实接口填写 `.env` 和字段映射。该变化不应影响系统其余模块。

虽然首版是私用且通过 SSH 隧道访问，仍应保留密码哈希、对象级权限、会话过期、TLS 验证、Key 不入库/不进 Git 和基本审计。这些措施实现成本低，且能够防止本机、服务器日志和代码仓库中的凭据与业务数据意外泄露。
